# ==============================================
# EJEMPLO DE VARIABLES DE ENTORNO - PRODUCCIÓN
# ==============================================
# Copia este archivo a .env y completa con valores reales
# NUNCA subas el archivo .env real a Git

# Puerto del servidor (solo accesible localmente)
PORT=3000

# Host - IMPORTANTE: usar 127.0.0.1 para que solo sea accesible localmente
HOST=127.0.0.1

# Entorno de ejecución
NODE_ENV=production

# URL del frontend para CORS (sin trailing slash)
FRONTEND_URL=https://easyinjection.com.mx
BASE_URL_FRONTEND=https://easyinjection.com.mx

# Base de datos MongoDB
EASYINJECTION_DB=mongodb://localhost:27017/easyinjection

# JWT - Clave privada (genera una clave fuerte y única)
# Ejemplo: openssl rand -base64 64
easyinjection_dev_jwtPrivateKey_2024=TU_CLAVE_JWT_SUPER_SECRETA_AQUI

# Email - Configuración para notificaciones
EMAIL_USER=tu-correo@gmail.com
EMAIL_PASS=tu-app-password-aqui

# Herramientas de escaneo - Rutas absolutas
SQLMAP_PATH=/usr/bin/sqlmap
SQLMAP_TIMEOUT=300000
DALFOX_PATH=/usr/local/bin/dalfox

# Configuración de CORS (opcional si necesitas limitar orígenes)
# ALLOWED_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com
